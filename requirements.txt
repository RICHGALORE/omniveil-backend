# Omni Veil Backend — Python dependencies
# Install: pip install -r requirements.txt
# Python 3.11+ required (developed on 3.14)

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Settings & environment
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0

# HTTP (for Sightengine AI detection)
httpx>=0.27.0

# File upload support (required by FastAPI Form + File)
python-multipart>=0.0.12

# Rate limiting
slowapi>=0.1.9

# Image processing & watermarking
Pillow>=11.0.0
numpy>=2.0.0
scipy>=1.14.0

# Perceptual hashing
imagehash>=4.3.1

# Cryptographic hashing
blake3>=1.0.0

# Email validation (required by pydantic EmailStr)
email-validator>=2.0.0

# Metadata extraction
exifread>=3.0.0      # EXIF from images
mutagen>=1.47.0      # Audio tags (ID3, Vorbis, etc.)
pypdf>=5.0.0         # PDF metadata
